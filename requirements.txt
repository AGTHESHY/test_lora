# ===================================================================
# 注意:请先单独安装 Blackwell (RTX 50 系) 可用的 PyTorch,再装本文件。
#
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision
#
# 不要用普通的 `pip install torch`,否则 RTX 5080 会报
# "no kernel image is available for execution on the device"。
# ===================================================================

# 核心:HuggingFace 生态
transformers>=4.46.0
peft>=0.13.0
accelerate>=1.0.0
datasets>=3.0.0

# QLoRA 4-bit 量化(Blackwell 需要较新版本)
bitsandbytes>=0.45.0

# SFT / 偏好优化训练器(SFTTrainer、后续 DPOTrainer 等)
trl>=0.12.0

# 分词与工具
sentencepiece>=0.2.0
tokenizers>=0.20.0

# 进度与可视化
tqdm>=4.66.0
